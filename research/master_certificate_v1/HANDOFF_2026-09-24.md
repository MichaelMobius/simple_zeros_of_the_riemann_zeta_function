# HANDOFF — simple zeros project / master-certificate experiment

Date: 2026-09-24 (Lima)

This file is the continuation state for a new ChatGPT session. It separates the **published/main theorem** from the **research-only nine-point hybrid experiment**. Do not merge the research claim into `paper/main.tex` until every open proof obligation below is closed.

## 1. Repository and branches

Repository:

`MichaelMobius/simple_zeros_of_the_riemann_zeta_function`

Canonical publication branch:

`main`

Current `main` head at handoff:

`e70dc380502dfab0d405e122dcc4668a4762a2f2`

Research branch:

`research/master-certificate-v1`

Current research head at handoff:

`3a8d097a4b5217790b90d30728dfd27b923ed1a7`

Relationship at handoff:

- research branch is 32 commits ahead of `main`;
- research branch is 0 commits behind `main`;
- all research changes are isolated from the publication branch.

Pinned Anthropic/formal-math base used by the Lean harness:

`fbdc36bbf17d20af3fd0447c6d1a8a02773c9844`

Pinned upstream nine-point certificate repository/commit:

`trmdy/zeta-simple-zeros-673137@1610b97b7895ff34982260f8dcaf04a0f7b82cf7`

## 2. Current theorem on `main`

The current paper proves the unconditional dyadic bound

\[
\liminf_{T\to\infty}
\frac{N_0^s(T,2T)}{N(T,2T)}
\ge
\frac{1125000H_{\rm MT}-2220}{1120671}
=0.6731175265883904388095857434106058666\ldots
\]

with

\[
H_{\rm MT}=\frac32-\frac1{\sqrt2}\cot\frac1{\sqrt2}.
\]

This is not RH and does not assert that all zeros are simple.

The publication theorem path has an exact rational finite closure

`44100 -> 511 -> 231 -> 23 -> 5 -> contradiction`

and a literal Lean endpoint for the `liminf`. Arb/FLINT is retained as an independent reproducibility check rather than a premise of the final Lean theorem path.

The relevant publication files include:

- `paper/main.tex` — current article.
- `paper/main.pdf` — compiled article.
- `paper/README.md` — manuscript summary.
- `paper/REPRODUCIBILITY.md` — publication snapshot.
- `lean_harness/v21_analytic/V26E3GlobalClosure.lean` — seven-point/hard-core closure bridge.
- `lean_harness/v21_analytic/V26PublishedLiminf.lean` — literal `liminf` endpoint.
- `certification/v26_exact/` — exact-rational finite certificate generators.
- `.github/workflows/v26-published-liminf.yml` — theorem build / axiom audit.
- `.github/workflows/v26-final-trust-audit.yml` — final trust audit.
- `.github/workflows/v26-final-cold-audit.yml` — cold rebuild audit.
- `.github/workflows/paper-v26-pdf.yml` — manuscript PDF build.

Do not rewrite `main` while developing the new research candidate.

## 3. Research objective

The research branch tests a hybrid of:

1. the pinned **nine-point local certificate** from `trmdy/zeta-simple-zeros-673137`;
2. Hurtado **adjacent-pair pinching**;
3. Hurtado **spectral-threshold contradiction**;
4. an independent reconstruction of the upstream trigonometric window/kernel in exact arithmetic and Lean;
5. eventual composition through the same global `liminf` endpoint.

The goal is to replace the upstream generic block assembly by a stronger block argument while re-proving the analytic pieces rather than importing the arbitrary-window interface as a black box.

## 4. Pinned nine-point local data

For the upstream window, with `w = k_v^2`, the pinned local certificate uses

```text
q       = 8
p       = 1/2500
epsilon = 15211/2500000
beta    = q*p = 2/625
H_cert  = 3362285207/5000000000
```

All eight span capacities are exactly `2`.

After summing through an `m`-point block,

\[
E+P\ge A:=\epsilon(m-8),
\qquad
\sum_r q_r=Q:=\beta(m-8),
\qquad
0\le q_r\le\beta.
\]

The Hurtado adjacent-pair mechanism gives

\[
D\ge\sum_r w(g_r),
\]

and if

\[
w(g)+tqg\ge tqg_0,
\]

then

\[
D+tP\ge tg_0Q.
\]

Together with

\[
D<E\Longrightarrow D>\frac{m}{m-1},
\]

this yields `D+P >= A` whenever

\[
g_0Q+\left(1-\frac1t\right)\frac{m}{m-1}-A>0.
\]

## 5. Robust operating point currently selected

Use

\[
g_0=\frac{43}{50}=0.86,
\qquad
t=\frac{31}{2},
\qquad
m=289.
\]

Then

\[
A=\frac{4274291}{2500000}=1.7097164,
\qquad
Q=\frac{562}{625}=0.8992.
\]

The exact contradiction margin is

\[
g_0Q+\left(1-\frac1t\right)\frac{289}{288}-A
=\frac{405889}{174375000}>0.
\]

The scalar kernel requirement reduces to

\[
k_v\!\left(\frac{43}{50}\right)>\frac{521}{2500}=0.2084,
\]

because

\[
\left(\frac{521}{2500}\right)^2-
\frac{1333}{31250}
=\frac{4841}{6250000}>0.
\]

## 6. Projected candidate constant

If all imported/local proof obligations are independently closed, the hybrid block theorem would give

\[
\frac SN\ge
\frac{289H_{\rm cert}-562/625}
     {289-4274291/2500000}
=
\frac{967204424823}{1436451418000}
\]

and hence

\[
\boxed{0.6733290194872431112041966740\ldots}.
\]

This exceeds:

```text
current Hurtado theorem       0.6731175265883904...
upstream 9-point assembly     0.6733127422722459...
Shi 2-certificate candidate   0.6733169771424713...
```

Important: **0.673329019487... is not yet a theorem and must not be placed in `paper/main.tex`.**

## 7. Research files — complete inventory

### A. Research notes / exact arithmetic

`research/master_certificate_v1/README.md`

Current research ledger and mathematical explanation. Note: one paragraph still describes an earlier six-job grouping plan; the live full-replay workflow now uses 96 matrix jobs, one per original shard, with `max-parallel: 24`.

`research/master_certificate_v1/prototype.py`

Discovery script for the hybrid operating point and projected constant. Use for exploration only; exact statements are duplicated in Lean where appropriate.

`research/master_certificate_v1/verify_window_kernel_086.py`

Exact-rational interval reproduction of the upstream seven-term trigonometric window kernel at `g=43/50`. Uses only `Fraction`, rational bounds for `pi` and `1/sqrt(2)`, and alternating Taylor bounds.

Current exact interval reproduced there:

\[
k_v(43/50)\in
[0.208459268877159404,\ 0.208459354615668058],
\]

so in particular

\[
k_v(43/50)>521/2500.
\]

`research/master_certificate_v1/replay_upstream_nine_point_shard.py`

Early/pilot shard replay helper.

`research/master_certificate_v1/replay_upstream_nine_point_group.py`

Current replay helper used by the full workflow. It checks out the pinned upstream certificate/verifier and replays an original shard.

`research/master_certificate_v1/HANDOFF_2026-09-24.md`

This file.

### B. Research Lean files

`lean_harness/research/ResearchNinePointHybrid.lean`

Green exact rational scalar core. Proves:

- `research9_contradiction_margin_exact`;
- positivity of the block contradiction margin;
- `research9_kernel_square_margin`;
- abstract scalar finisher `research9_strong_block_scalar`;
- exact projected-constant identity;
- projected bound is greater than `0.673329`.

It deliberately does **not** prove the upstream nine-point certificate or arbitrary-window analytic interface.

`lean_harness/research/ResearchWindowKernel086v2.lean`

Green Lean proof of the transcendental/rational closed-form inequality at `x=43/50`. It uses alternating sine/cosine Taylor estimates and Mathlib bounds for `pi`; it proves the strict closed-form lower bound required for the scalar step. The file itself explicitly notes that a separate bridge is needed to identify the closed form with the integral-defined overlap kernel.

`lean_harness/research/ResearchWindowKernelBridge.lean`

Green generic integral bridge. Kernel-checks the cosine-overlap identity

\[
\int_{-1/2}^{1/2}\cos(as)\cos(bs)\,ds
=
\frac{\sin((a-b)/2)}{a-b}
+
\frac{\sin((a+b)/2)}{a+b},
\]

plus the one-frequency normalization and vanishing of nonzero integer Fourier modes.

`lean_harness/research/ResearchWindowActual.lean`

Connects the actual pinned seven-term upstream profile

\[
\cos(\sqrt2 s)
+0.0033225\cos(2\pi s)
-0.007609135\cos(4\pi s)
+0.001190194\cos(6\pi s)
-0.000731476\cos(8\pi s)
-0.001680572\cos(10\pi s)
+0.001141360\cos(12\pi s)
\]

to the closed normalization. It proves

`research9_window_norm_integral_eq_closed`.

At handoff, the analogous **numerator/overlap identity at `x=43/50` is still open**.

### C. Research workflows

`.github/workflows/research-nine-point-hybrid.yml`

Builds the research Lean scalar/kernel/integral modules and reports axioms. The original scalar-core run `35803843979` completed successfully.

`.github/workflows/research-nine-point-replay-pilot.yml`

Pinned independent replay pilot. Shard `0/96` completed successfully in run `35943514827` with

```text
verified        = True
nodes           = 658969
pruned          = 329486
splits          = 329483
maximum_depth   = 45
elapsed_seconds = 723.233
```

and reproduced upstream interval-table hashes

```text
w   = 2be38c2f2a5a4659200341fae9b2a926760e64ad04fa44ae214a77dd958c56e3
w'' = 580e9682f1171ead19595438ee83c9f4c5af837179060ec49d9faee88032f6d5
```

`.github/workflows/research-nine-point-replay-full.yml`

Current full replay. At handoff it uses a 96-element matrix (`shard: 0..95`) with `max-parallel: 24`; each job checks out the exact upstream commit, installs the pinned verifier, and replays one original shard. Latest run at handoff:

`36079044711`

Status at the time this handoff was written: **in progress**. Do not mark the upstream nine-point certificate independently replayed until all 96 jobs finish with success.

## 8. Proof ledger at handoff

### Closed / green

- Current `main` theorem `0.673117526588...` remains unchanged and publication-ready.
- Research hybrid block rational algebra is green in Lean.
- Projected final rational constant arithmetic is green in Lean.
- Exact-rational signed-kernel enclosure at `43/50` is green.
- Closed-form signed-kernel lower bound is green in Lean.
- Generic cosine-overlap integral bridge is green in Lean.
- Integer-frequency zero-mean normalization identities are green in Lean.
- Actual seven-term window normalization integral is identified with the closed normalization in Lean.
- Independent replay pilot for shard `0/96` is green.

### In progress

- Full independent replay of all 96 upstream certificate shards: workflow run `36079044711`.

### Open

1. Finish Lean numerator identity for the actual seven-term window at `x=43/50`, identifying the integral-defined overlap kernel with the closed form already bounded in `ResearchWindowKernel086v2.lean`.
2. Compose that identity with the strict lower-bound theorem to obtain an integral-kernel theorem `k_v(43/50) > 521/2500` in the actual-window namespace.
3. Prove positivity of the actual pinned window on `[-1/2,1/2]` (or another sufficient hypothesis) and package monotonicity of `k_v(g)` for `0 <= g <= 43/50`.
4. Abstract/generalize the current adjacent-pair and spectral-threshold Lean modules so they apply to this window cleanly, or instantiate them directly with the research window.
5. Prove the upstream window satisfies the analytic hypotheses required by the manuscript's Appendices III--IV; do not import the upstream arbitrary-window Gabor interface as an unproved black box if the goal is an independent theorem.
6. Once the 96-shard replay is fully green, encode the nine-point local inequality as an internal Lean theorem/interface with exact rational coefficients/capacities.
7. Compose the local nine-point theorem + actual-window kernel theorem + generalized strong-block theorem into a global defect bound.
8. Push the generalized global bound through the literal `liminf` endpoint, with the same trust/axiom audit standard used on `main`.
9. Only then decide whether the new result belongs in `paper/main.tex` or a separate paper/version.

## 9. Recommended next action

Do **not** spend effort optimizing the decimal constant yet. The cleanest next mathematical target is the exact Lean theorem

\[
\frac{
\int_{-1/2}^{1/2}
  \mathrm{research9Window}(s)\cos(2\pi(43/50)s)\,ds
}{
\int_{-1/2}^{1/2}\mathrm{research9Window}(s)\,ds
}
>
\frac{521}{2500}.
\]

The denominator identity is already closed in `ResearchWindowActual.lean`; the remaining task is the numerator expansion into the seven cosine-overlap terms and composition with `ResearchWindowKernel086v2.lean`.

In parallel, allow workflow run `36079044711` to finish the 96-shard replay. If any shard fails, inspect that shard before changing the verifier or certificate.

## 10. Important guardrails

- Never claim RH.
- Do not call the development axiom-free; the standard Lean/Mathlib axiom closure is expected to be `propext`, `Classical.choice`, `Quot.sound`.
- Keep `main` and the research branch conceptually separate.
- Do not report `0.673329019487...` as an established zeta-zero bound yet.
- Do not silently import the upstream arbitrary-window theorem if the aim is an independent proof.
- Do not merge the research branch into `main` until the proof ledger is fully closed.
- Preserve exact upstream pins and certificate hashes in every replay/audit.

## 11. Minimal prompt for the next chat

Paste or say:

> Continue the Riemann-zeta simple-zero project from `research/master_certificate_v1/HANDOFF_2026-09-24.md` on branch `research/master-certificate-v1`. Do not modify `main`. First check workflow run `36079044711`; then continue the actual-window numerator identity at `x=43/50` and compose it with `ResearchWindowKernel086v2.lean`. Keep the candidate `0.6733290194872431...` research-only until the 96-shard certificate replay and the independent analytic window interface are fully closed.
